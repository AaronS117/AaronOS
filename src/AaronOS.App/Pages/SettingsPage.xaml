<Page
    x:Class="AaronOS_App.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d">

    <Border Padding="24">
        <StackPanel MaxWidth="360" HorizontalAlignment="Left">
            <TextBlock Text="Settings" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16" />

            <TextBlock Text="Height (in)" Margin="0,0,0,4" />
            <ui:NumberBox Value="{Binding HeightInches, Mode=TwoWay}" Margin="0,0,0,16" />

            <ui:Button Content="Save" Command="{Binding SaveCommand}" Appearance="Primary" />
            <TextBlock Text="{Binding StatusMessage}" Opacity="0.7" Margin="0,8,0,0" />
        </StackPanel>
    </Border>
</Page>
