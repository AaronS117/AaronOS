<Page
    x:Class="AaronOS.Modules.BodyMeasurements.Views.ClothingSizesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:data="clr-namespace:AaronOS.Modules.BodyMeasurements.Data"
    mc:Ignorable="d">

    <Border Padding="24">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,32,0">
                <TextBlock Text="Clothing Sizes" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,12" />

                <TextBlock Text="Category" Margin="0,0,0,4" />
                <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding NewCategory, Mode=TwoWay}" Margin="0,0,0,12" />

                <TextBlock Text="Size" Margin="0,0,0,4" />
                <ui:TextBox Text="{Binding NewSizeLabel, Mode=TwoWay}" PlaceholderText="e.g. 34x32, M, 9.5" Margin="0,0,0,12" />

                <TextBlock Text="Brand (optional)" Margin="0,0,0,4" />
                <ui:TextBox Text="{Binding NewBrand, Mode=TwoWay}" Margin="0,0,0,12" />

                <TextBlock Text="Notes (optional)" Margin="0,0,0,4" />
                <ui:TextBox Text="{Binding NewNotes, Mode=TwoWay}" Margin="0,0,0,12" />

                <ui:Button Content="Add" Command="{Binding AddCommand}" Appearance="Primary" />
            </StackPanel>

            <ListView Grid.Column="1" ItemsSource="{Binding Entries}">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type data:ClothingSizeEntry}">
                        <Grid Margin="0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Date}" Margin="0,0,16,0" />
                            <TextBlock Grid.Column="1" Text="{Binding Category}" Margin="0,0,16,0" />
                            <TextBlock Grid.Column="2" Text="{Binding SizeLabel}" FontWeight="SemiBold" Margin="0,0,16,0" />
                            <TextBlock Grid.Column="3" Text="{Binding Brand}" Opacity="0.7" Margin="0,0,16,0" />
                            <TextBlock Grid.Column="4" Text="{Binding Notes}" Opacity="0.7" TextTrimming="CharacterEllipsis" Margin="0,0,16,0" />
                            <Button Grid.Column="5" Content="Delete" Click="DeleteButton_Click" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</Page>
