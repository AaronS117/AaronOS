<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AaronOS.Modules.BodyMeasurements.Views.ClothingSizesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:AaronOS.Modules.BodyMeasurements.Data"
    mc:Ignorable="d">

    <Grid Padding="24" ColumnSpacing="32">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Spacing="12">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Clothing Sizes" />
            <ComboBox Header="Category" ItemsSource="{x:Bind ViewModel.Categories}"
                      SelectedItem="{x:Bind ViewModel.NewCategory, Mode=TwoWay}" />
            <TextBox Header="Size" Text="{x:Bind ViewModel.NewSizeLabel, Mode=TwoWay}" PlaceholderText="e.g. 34x32, M, 9.5" />
            <TextBox Header="Brand (optional)" Text="{x:Bind ViewModel.NewBrand, Mode=TwoWay}" />
            <TextBox Header="Notes (optional)" Text="{x:Bind ViewModel.NewNotes, Mode=TwoWay}" />
            <Button Content="Add" Command="{x:Bind ViewModel.AddCommand}" Style="{StaticResource AccentButtonStyle}" />
        </StackPanel>

        <ListView Grid.Column="1" ItemsSource="{x:Bind ViewModel.Entries}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:ClothingSizeEntry">
                    <Grid ColumnSpacing="16" Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Date}" />
                        <TextBlock Grid.Column="1" Text="{x:Bind Category}" />
                        <TextBlock Grid.Column="2" Text="{x:Bind SizeLabel}" FontWeight="SemiBold" />
                        <TextBlock Grid.Column="3" Text="{x:Bind Brand}" Opacity="0.7" />
                        <TextBlock Grid.Column="4" Text="{x:Bind Notes}" Opacity="0.7" TextTrimming="CharacterEllipsis" />
                        <Button Grid.Column="5" Content="Delete" Click="DeleteButton_Click" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
