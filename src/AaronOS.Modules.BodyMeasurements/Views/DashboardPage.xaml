<Page
    x:Class="AaronOS.Modules.BodyMeasurements.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:AaronOS.Modules.BodyMeasurements.ViewModels"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel MaxWidth="720" HorizontalAlignment="Left">
            <TextBlock Text="Body Measurements" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,20" />

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <StackPanel Margin="0,0,32,0">
                    <TextBlock Text="Latest weight" Opacity="0.7" />
                    <TextBlock Text="{Binding LatestWeightLb, TargetNullValue='—'}" FontSize="20" FontWeight="SemiBold" />
                </StackPanel>
                <StackPanel Margin="0,0,32,0">
                    <TextBlock Text="BMI" Opacity="0.7" />
                    <TextBlock Text="{Binding Bmi, TargetNullValue='Set height in Settings'}" FontSize="20" FontWeight="SemiBold" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Last check-in" Opacity="0.7" />
                    <TextBlock Text="{Binding LatestCheckInDate, TargetNullValue='No check-ins yet'}" FontSize="20" FontWeight="SemiBold" />
                </StackPanel>
            </StackPanel>

            <TextBlock Text="Weight trend" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
            <lvc:CartesianChart
                Height="260"
                Series="{Binding WeightSeries}"
                XAxes="{Binding WeightAxes}"
                Margin="0,0,0,20" />

            <TextBlock Text="Active goals" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
            <ItemsControl ItemsSource="{Binding ActiveGoals}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:GoalProgress}">
                        <ui:Card Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="{Binding Label}" FontWeight="SemiBold" />
                                <ProgressBar Value="{Binding ProgressFraction}" Maximum="1" Margin="0,4" />
                                <TextBlock Text="{Binding ProgressText}" Opacity="0.7" FontSize="12" />
                            </StackPanel>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</Page>
