<Page
    x:Class="AaronOS.Modules.BodyMeasurements.Views.CheckInPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel MaxWidth="480" HorizontalAlignment="Left">
            <TextBlock Text="Check In" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16" />

            <TextBlock Text="Date" Margin="0,0,0,4" />
            <DatePicker SelectedDate="{Binding Date, Mode=TwoWay}" Margin="0,0,0,12" />

            <TextBlock Text="Weight (lb)" Margin="0,0,0,4" />
            <ui:NumberBox Value="{Binding WeightLb, Mode=TwoWay}" Margin="0,0,0,16" />

            <TextBlock Text="Measurements (in)" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,12">
                    <TextBlock Text="Neck" Margin="0,0,0,4" />
                    <ui:NumberBox Value="{Binding NeckIn, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
                    <TextBlock Text="Chest" Margin="0,0,0,4" />
                    <ui:NumberBox Value="{Binding ChestIn, Mode=TwoWay}" />
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,12">
                    <TextBlock Text="Waist" Margin="0,0,0,4" />
                    <ui:NumberBox Value="{Binding WaistIn, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,12">
                    <TextBlock Text="Hips" Margin="0,0,0,4" />
                    <ui:NumberBox Value="{Binding HipsIn, Mode=TwoWay}" />
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,12,12">
                    <TextBlock Text="Bicep (L)" Margin="0,0,0,4" />
                    <ui:NumberBox Value="{Binding BicepLeftIn, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="1" Margin="0,0,0,12">
                    <TextBlock Text="Bicep (R)" Margin="0,0,0,4" />
                    <ui:NumberBox Value="{Binding BicepRightIn, Mode=TwoWay}" />
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,12,12">
                    <TextBlock Text="Thigh (L)" Margin="0,0,0,4" />
                    <ui:NumberBox Value="{Binding ThighLeftIn, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Grid.Row="3" Grid.Column="1" Margin="0,0,0,12">
                    <TextBlock Text="Thigh (R)" Margin="0,0,0,4" />
                    <ui:NumberBox Value="{Binding ThighRightIn, Mode=TwoWay}" />
                </StackPanel>

                <StackPanel Grid.Row="4" Grid.Column="0" Margin="0,0,12,12">
                    <TextBlock Text="Calf (L)" Margin="0,0,0,4" />
                    <ui:NumberBox Value="{Binding CalfLeftIn, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel Grid.Row="4" Grid.Column="1" Margin="0,0,0,12">
                    <TextBlock Text="Calf (R)" Margin="0,0,0,4" />
                    <ui:NumberBox Value="{Binding CalfRightIn, Mode=TwoWay}" />
                </StackPanel>
            </Grid>

            <TextBlock Text="Notes" Margin="0,4,0,4" />
            <TextBox Text="{Binding Notes, Mode=TwoWay}" AcceptsReturn="True" Height="72" Margin="0,0,0,16" />

            <ui:Button Content="Save Check-In" Command="{Binding SaveCommand}" Appearance="Primary" />
            <TextBlock Text="{Binding StatusMessage}" Opacity="0.7" Margin="0,8,0,0" />
        </StackPanel>
    </ScrollViewer>
</Page>
