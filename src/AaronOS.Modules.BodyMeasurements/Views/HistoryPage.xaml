<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AaronOS.Modules.BodyMeasurements.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:AaronOS.Modules.BodyMeasurements.Data"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="History" />

        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.CheckIns}" MaxWidth="720" HorizontalAlignment="Left">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:BodyCheckIn">
                    <Grid ColumnSpacing="16" Padding="0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{x:Bind Date}" />
                        <TextBlock Grid.Column="1" Text="{x:Bind WeightLb}" />
                        <TextBlock Grid.Column="2" Text="{x:Bind Notes}" Opacity="0.7" TextTrimming="CharacterEllipsis" />
                        <Button Grid.Column="3" Content="Delete" Click="DeleteButton_Click" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
