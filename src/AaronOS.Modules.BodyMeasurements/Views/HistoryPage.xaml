<Page
    x:Class="AaronOS.Modules.BodyMeasurements.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:data="clr-namespace:AaronOS.Modules.BodyMeasurements.Data"
    mc:Ignorable="d">

    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="History" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,16" />

            <ListView Grid.Row="1" ItemsSource="{Binding CheckIns}" HorizontalAlignment="Stretch">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type data:BodyCheckIn}">
                        <Grid Margin="0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Date}" Margin="0,0,16,0" />
                            <TextBlock Grid.Column="1" Text="{Binding WeightLb}" Margin="0,0,16,0" />
                            <TextBlock Grid.Column="2" Text="{Binding Notes}" Opacity="0.7" TextTrimming="CharacterEllipsis" Margin="0,0,16,0" />
                            <Button Grid.Column="3" Content="Delete" Click="DeleteButton_Click" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</Page>
