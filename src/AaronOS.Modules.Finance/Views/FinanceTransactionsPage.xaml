<Page
    x:Class="AaronOS.Modules.Finance.Views.FinanceTransactionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:data="clr-namespace:AaronOS.Modules.Finance.Data"
    mc:Ignorable="d">

    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Transactions" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,12" />

            <ComboBox Grid.Row="1" ItemsSource="{Binding Accounts}" SelectedItem="{Binding SelectedAccount, Mode=TwoWay}"
                      DisplayMemberPath="Name" Width="240" HorizontalAlignment="Left" Margin="0,0,0,12" />

            <ListView Grid.Row="2" ItemsSource="{Binding Transactions}" MaxWidth="720" HorizontalAlignment="Left">
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type data:FinanceTransaction}">
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Date}" />
                            <TextBlock Grid.Column="1" Text="{Binding Name}" Margin="8,0" TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="2" Text="{Binding CategoryPrimary}" Opacity="0.7" />
                            <TextBlock Grid.Column="3" Text="{Binding Amount, StringFormat=C}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</Page>
